# Keep the store deterministic and hoisting predictable across the workspace.
auto-install-peers=true
strict-peer-dependencies=false
# Only allow explicitly declared dependencies to be imported (no phantom deps).
shamefully-hoist=false
